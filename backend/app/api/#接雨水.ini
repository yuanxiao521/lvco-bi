#接雨水
    left = 0 , right = len(nums) - 1 
    left_max = 0 , right_max = 0    
    water = 0
while left < right:
    if height[left]< height[right]:
        if height[left]>left_max:
            left_max=height[left]
        left+=1
        else:
        
    else:
        if height[right]>=right_max:
            right_max = height[right]
            
        right -= 1

            
#滑动窗口最大值
  n = len(nums)
  head = 0 
  q = []
  
  for i in range(n):
    while q and len(q) > head and nums[q[-1]] <=nums[i]:
        q.pop()
    q.append(i)

    #队头过期，弹出队头元素
    while q and q[head] <= i-k:
        head +=1 

    while i >= k-1:
        result.append(q[head])
    

